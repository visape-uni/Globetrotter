<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/action_add_fav"
        android:icon="@drawable/ic_no_favorite"
        android:title="@string/action_add_fav"
        android:iconTint="@color/colorWhite"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/action_rmv_fav"
        android:icon="@drawable/ic_favorite"
        android:title="@string/action_rmv_fav"
        android:iconTint="@color/colorWhite"
        android:visible="false"
        app:showAsAction="ifRoom"/>
</menu>